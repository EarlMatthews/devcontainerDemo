{
    "name": "Minimal Devcontainer",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "customizations": {
      "vscode": {
        // This extension is included to provide Docker support within the devcontainer.
        "extensions": ["ms-azuretools.vscode-docker"]
      }
    },
    "forwardPorts": []
  }